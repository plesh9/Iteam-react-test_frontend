import type { FC } from 'react'
import { Box } from 'vibe-library'
import s from './Header.module.scss'

const Header: FC = () => {
    return <Box className={s.main}>Header</Box>
}

export default Header
